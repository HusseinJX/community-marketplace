# Pending / TODO (full)

Split out of CLAUDE.md. CLAUDE.md keeps only the START HERE block.

## Pending / TODO

### ▶ START HERE (set 2026-08-10) — three verification actions
The gap is no longer code. Seven commerce features are shipped, deployed and correct in arithmetic, in code and against the real database; three have never been proven against reality. In order:
1. **Charge ONE real card** — validates ticketing, shop pickup, self-delivery AND digital delivery at once. ⚠️ `.env.local` holds **live** Stripe keys, so a local checkout charges for real; add `sk_test_`/`pk_test_` first to rehearse.
2. **Verify Square with a SANDBOX token** — `npx tsx scripts/square-smoke.mts <sandbox-token>`. Free, zero risk, do it **before any vendor connects**. Needs FOUR scopes: `APPOINTMENTS_READ`, `APPOINTMENTS_WRITE`, `ITEMS_READ`, `CUSTOMERS_WRITE`.
3. **Connect Printify** (Integrations → Print on demand) — the first real connect IS the test.

**Rules that must not be relaxed** (every bug in that session came from relaxing one): the **server derives, the client never decides** (prices from the catalog, fulfillment from the basket, fees from the vendor's rules); and **whoever pays the carrier keeps the fee** (uber → platform, self → vendor, printify → vendor), with the 5% on **items only, always**. After any commerce change, audit specifically for the two shapes that produced silent bugs: **trusting a caller for something the server can derive**, and **an active-only product lookup used AFTER payment** (`getProductsByMember` filters `active`; delivery must use `getAllProductsByMember` — they paid, so whether it's still listed is irrelevant).

### ⚠️ Outstanding TESTS (2026-08-10) — what is built but not proven
Ordered by risk. Everything below is correct in code and against the database; these are the gaps between that and reality.
1. **NO REAL CARD HAS EVER BEEN CHARGED.** Ticketing, shop pickup, self-delivery, Printify postage and digital delivery all compute money correctly in arithmetic and in code, and none has ever moved a cent through Stripe. **ONE live purchase validates four of them at once** — it is the highest-value ten minutes available. ⚠️ **`.env.local` holds LIVE Stripe keys (`sk_live_`/`pk_live_`)**, so a local checkout test creates a REAL charge; add `sk_test_`/`pk_test_` locally to rehearse safely (same trap as the Clerk `pk_live`-on-localhost gotcha).
2. **Printify has never touched its live API** — no account connected, so `listShops`/`listProducts`/`shippingCost`/`createOrder` are unexercised. First connect is the test. Paste a token into Integrations → Print on demand.
3. **The ticket email has never been sent through Resend for real** (the sending path is verified elsewhere, this specific template isn't).
4. **The door check-in screen has never been seen rendered** — `isAdmin`/host auth redirects a headless session. Sign in and open `/vendor/checkin/<eventId>`.
5. **The vendor-facing Delivery and Printify cards have not been seen rendered** (same auth reason). Buyer-side checkout WAS driven in a browser for pickup, self-delivery, service and digital baskets.
6. **Uber Direct**: still blocked on account activation at Uber's end; re-run the quote probe before pushing `UBER_DIRECT_*` to prod.

### ⛔ NOT DEPLOYED — everything from 2026-08-22 (set 2026-08-22)
**32 commits (`1aa1452..f102d12`) are committed and NOT deployed.** Prod is still running
~`1aa1452`, which means prod still has: the broken image optimizer, the 12 demo products in
the shop, the "Pro — sell online $30/mo" plan card, and pickup offered with no arrangement.
The single highest-value item in this file is now **deploy**, and the demo gate is mandatory:
`npm run build` → `PORT=3100 npx next start` → `curl -sI :3100/vendor` **must** be 307.

Deploy-blocked follow-ups, in order:
1. **The sharp fix only takes effect on deploy.** Until then every image on prod ships at full
   source resolution. Verify after deploying: `curl -sI 'https://whatslocal.ai/_next/image?url=%2Flogo.png&w=256&q=75'` must return `content-type: image/avif`, not `image/png`.
2. **Xeno cannot currently be bought from.** Its four tees are the entire storefront, and it has
   no pickup arrangement and no delivery — so checkout correctly refuses. Add a pickup note in
   Integrations → Delivery (or turn on self-delivery) before testing a real card.
3. **`JOINDEMO_PASSWORD` fails closed in production** (no default, by design). Confirm it is set
   on CapRover or `/joindemo` can never be unlocked for anyone you send the link to.

### Outstanding TASKS (2026-08-22 additions)
- **AI product suggestions from Google Maps photos — NOT BUILT, needs a decision.** Asked for,
  then narrowed to "add products with AI", which shipped (photograph a menu → `/api/ai/extract`).
  The Maps half was left out deliberately: **nothing in this app fetches Places photos** —
  `lib/places.ts` has no photo code, and the images already on a member profile come from the
  connector's own bucket (`zahabbucket.nyc3.digitaloceanspaces.com`), not Google. Doing it for
  real means new billable Places Photo calls AND repurposing Google-licensed photos into stored,
  model-derived product images, which their terms do not obviously allow. Use the connector's
  own photos instead, or decide the Google path deliberately.
- **`/joindemo` cannot show the one failure that matters.** In the real flow, a listing with no
  verifiable phone bounces back to the search with "This listing has no phone we can verify" —
  the most common real dead end. The demo marches straight to the code screen. A few lines to
  detect it and show the real error; worth doing before showing the demo to anyone.
- **`deviceSizes` caps at 1280**, so a full-width hero on a 2x desktop display (wants ~2560px)
  tops out and may look soft now that the optimizer actually resizes. Adding 1536/1920 costs
  bytes and decode memory — weigh against the iOS OOM history before changing it.
- **`/api/events/feed` is 122KB of JSON**, much of it event descriptions the cards never render.
  Trimming the payload is the next real win on home after the hydration waterfall.
- **Home is `force-dynamic` + client-only**, so the sequence is HTML → JS → hydrate → fetch →
  render → images. Server-rendering the first payload into SWR `fallbackData` collapses roughly
  a second of dead time before the first image byte is requested.
- **The shop's `isCollectable()` is a heuristic** (`lib/fulfillment.ts`): a street number, or a
  named place with a locality after it, ZIP stripped first. It will wrongly refuse some real
  collection points — those vendors have the pickup-note field beside the address, which is the
  intended escape, but if refusals show up in support this is the place to look.
- **Bookings lost its slot on the onboarding "three ways" screen** (replaced by self delivery, as
  asked). Request-to-book still exists and still works; nothing surfaces it during onboarding
  any more.

### Outstanding TASKS
- ✅ **Request-to-book AND Square Appointments are BUILT (2026-08-10)** — see Bookings below. ⚠️ **Square's live API calls are UNVERIFIED** (no account connected), but `square_env` supports Square's **sandbox**, so this one CAN be verified without risking a real business: `npx tsx scripts/square-smoke.mts <sandbox-token>` exercises locations, services and availability for real. Do that before any vendor connects.
- **Digital**: re-enabled and live. **Printify**: live but unverified (above).
- **Commerce roadmap after ticketing (2026-08-10), in the order to do it.** (1) **Shop pickup needs ONE real card run** — it's built and the money math is correct, it has simply never been walked with a live charge. (2) ✅ **DONE — vendor-handled delivery** (see Self-delivery below). (3) ✅ **DONE — `kind`, digital delivery, and Printify POD** (see below). ⚠️ **Printify is the ONE feature never exercised against its live API** — nobody has connected an account. First connect is the real test. (4) **Services/bookings is NOT a commerce phase** — there is no availability or calendar subsystem, which is why a service still reads as "Pick up" (see "Services still fulfil approximately" below). Square POS via Composio does not solve that; scheduling deserves its own build. Uber Direct remains blocked on account activation at Uber's end.
- **Cities are ACTIVATED BY SOURCES (2026-08-05).** `lib/cities.ts` derives the live-city set from `lib/sources/registry.ts` — a city is live when ≥1 enabled source carries its `city` id. There is no "live" flag to flip: **adding the first Oakland source turns Oakland on**, and the home `CityHeader` follows automatically. `SourceDef` carries `city` + optional `enabled`. Admin → Sourcing (`components/admin/SourcingPanel.tsx` + `app/api/admin/sources`, isAdmin-gated) lists the REAL recipes with counts measured off the events they wrote — it previously rendered the prototype's six invented sources, so the screen shipped while the wiring never did. **Note:** a source showing 0 events is not necessarily broken — `somarts` reads 0 because SOMArts lists one event and listed none at seed time; its recipe passes a dry run. Verify with `npx tsx scripts/publish-events.ts --only <id> --dry-run` before treating a zero as drift. Adding a source is still a code edit + deploy (a source is a scraping recipe, not a form row).
- **✅ DONE 2026-08-05 — proximity ranking for businesses + posts (built + browser-verified, NOT yet deployed).** Spec: `session-context/2026-08-05-proximity-ranking-spec.md`. **Businesses** = ranking only, no migration: the Shop tab (`components/home/LocalDirectory.tsx`) sorts nearest-first via `useHomePosition()` + the new **`lib/proximity.ts`** (`milesTo`/`byDistance`/`milesLabel`, and `0,0` counts as unplaced), with a "Near me" radius pill and a distance chip on `MemberCard`. **Posts** = migration `20260805120000_post_coordinates` (nullable `lat`/`lng`, **applied to prod + registered**), captured in `ShareComposer` from the fix it already resolved, ranked in `CommunityFeed`; **forward-only** — geocoding the stored label stays REJECTED (a district centroid printed as the post's spot). **The rule in both:** a record with no coords never shows a distance, never ranks as if nearby, and is excluded (and counted out loud) by a radius filter rather than exempted. Also fixed: `LiveFeed`/`LiveNowRail` were calling `getUserPosition()` directly = a second permission dialog on the Feed tab. **Still open:** deploy it, and `/explore` + `/browse` still sort by name-match score (`lib/proximity.ts` is ready for both).
- **NEXT UP IS NATIVE — NFC fob → app deep link (Universal Links). NOT STARTED.** Two halves and **neither works alone**: (a) `/.well-known/apple-app-site-association` served from `whatslocal.ai` as `application/json`, **no redirect, no `.json` extension** — ships with a normal CapRover deploy; (b) `com.apple.developer.associated-domains` = `applinks:whatslocal.ai` in `App.entitlements` — entitlements are compiled into the IPA, so **Xcode rebuild + App Review**. Users on the current build keep getting Safari until they update and there is **no forced-update mechanism**, so shipping only the web half achieves nothing visible. Also staged-but-inactive: push `presentationOptions` needs `npx cap sync ios` + rebuild; the NFC tag-reading entitlement is **not linked in the pbxproj**; **ATT must be wired before the ad pixels are ever switched on**.
- **Five temporary hides, all one-entry reverts with render branches intact, no decision yet (2026-08-04/05):** the **whole Collabs section** on the vendor dashboard (`VendorHome.tsx` `SHOW_COLLABS` — set `true` to restore; typed `boolean`, NOT the literal `false`, or TS stops narrowing `memberId` inside and the build breaks), and within it the **Create** tab and **Join / "Opportunities near you"** (`tabs` array); the **Start selling** checklist (`app/vendor/page.tsx:169`); and **paste an event link** (`VendorHome.tsx`, commented out for the same narrowing reason).
- **Accepted as-is — do NOT "fix" unprompted (confirmed by the user 2026-08-04):** the **Book button is a UI-only demo** (no booking system exists), the **iOS share-sheet** isn't needed now, and the **orphan routes** (`/vendor/messages/assistant`, `/messages/assistant`, `/events`, unlinked shopper routes) are fine.
- **Video upload: one link unproven on prod (2026-08-04).** Everything else is verified — real upload to the `WhatsLocal AI` brand channel, real delete, real moderator purge, all against the live YouTube API and prod DB — but **an authenticated upload on prod has never been exercised.** `/api/share/upload` checks `auth()` *before* `youtubeConfigured()`, so an unauthenticated probe returns 401 either way and the 503→working flip is unobservable from outside. **To close it:** post a video on whatslocal.ai, then confirm a `youtube.com` URL landed in `posts.video_urls` on channel `UC9QE0QLOPBMVMk_SI59h0eg`.
- **iOS crash-and-reload = WKWebView OOM, and images are the driver (2026-08-11, fixed `044861a`).** The native app has no bundle of its own — it loads `https://whatslocal.ai` live via Capacitor `server.url` — so **a web deploy reaches every phone with no app rebuild**, and a memory regression on the web ships straight to iOS as an apparent crash. The cause was `ImageCarousel` picking `idx === 0 && priority ? "eager" : idx === 0 ? "eager" : "lazy"` — two identical branches, so `priority` was inert and the first slide of EVERY carousel was eager. Dormant while `usableImages()` filtered posters out; the moment `a43e9b3` made them display, the For-you feed fetched **60 full-width posters on mount** (~200MB decoded). Eager is now opt-in via `priority` (the member hero is the only caller). **If it recurs, the levers are documented at `PAGE` in `components/feed/PersonalizedEvents.tsx`:** cap the mobile `sizes` variant (quadratic win), lower `PAGE`, then virtualise. **`quality` is NOT a lever** — decoded bitmap RAM depends only on pixel dimensions, so lowering it just looks worse. **Diagnostic trick:** A/B a render change against **prod** (still running the old code) rather than rebuilding — same URL, count `loading="eager"`.
- **Video moderation gaps (2026-08-04):** (a) **`banAuthor()` only soft-removes** a banned user's posts, so their videos stay live on the channel by link — `purge` is per-post only; (b) `purge` is still **curl-only** — the Moderation UI added 2026-08-11 exposes remove/restore/ban and the AI verdicts, but not purge; (c) every user's video lands on **one company channel**, which inherits YouTube's copyright/strike exposure as UGC volume grows.
- **Scraped drafts UI has never been seen RENDERED (2026-08-04).** Live on prod and admin-gated; data, API and auth are all verified, but `isAdmin` redirects a headless session so the layout is unverified. Sign in and open `/vendor/admin?tab=drafts`.
- **Scraped events — LIVE, with these open items (2026-08-04).** Full write-up in `session-context/2026-08-02-event-sourcing-geo-reco.md` (**gitignored — local to Xen's machine only**, which is why the operational facts below live here instead).
  - **⚠️ NFC fob → native app deep link: NOT STARTED, and it is the one item needing a native change.** Universal Links is two halves and neither works alone: `/.well-known/apple-app-site-association` (web, ships via CapRover) **AND** `com.apple.developer.associated-domains` = `applinks:whatslocal.ai` in `App.entitlements` (**Xcode rebuild + App Review** — entitlements are compiled into the IPA). Users on the current build keep getting Safari until they update, and there is no forced-update mechanism. Don't start the web half expecting it to work by itself.
  - **✅ FIXED + VERIFIED IN PROD (2026-08-04). The sweep was broken and would have failed every night, silently.** Two faults, both invisible from reading code: (1) **Trigger.dev prod had ZERO env vars** — they were set on the **dev** environment, so every `db()` call threw `SUPABASE_URL / key not set`; (2) the deployed worker ran **Node 21, which has no native WebSocket**, and `@supabase/supabase-js` builds a Realtime client inside `createClient()`, so it died on boot — local runs (Node 25) passed with identical code. Fixes: the four vars set on prod, and `trigger.config.ts` pins **`runtime: 'node-22'`** (redeployed as **v20260804.4**). Verified by running the real thing in prod: a full-path run on `lacocina` returned `pulled 3 · kept 3 · written 3 · published 3 · failed 0 · labelled 5 · pending 0` (which also cleared the 5 unlabelled stragglers). **Lesson: `tr_dev_` in `.env.local` means everything you trigger from this machine — including env-var writes — lands in dev. Check the environment, not just the dashboard.** Prod runs are reachable with the prod key from `GET /api/v1/projects/<ref>/prod` using the CLI's PAT (`~/Library/Preferences/trigger/config.json`).
  - **`sync-all-catalogs` (composio, `trigger/composio.ts`) is deliberately DISABLED** — its declarative `cron` is commented out. It had never actually run (the worker was never deployed) and deploying the events sweep would have switched it on as a side effect of an unrelated feature. Restoring it is one line; do that on purpose, not by accident.
  - **✅ DONE — the 41 `downtownsf` drafts now have a review UI:** **Admin → Scraped drafts** (`components/admin/EventDrafts.tsx` + `lib/sources/review.ts` + `app/api/admin/event-drafts`). Migration `20260804150000` adds **`vendor_events.reviewed_at`**, which is what makes a **reject** possible at all: `active=false` alone can't tell "unreviewed" from "rejected", and deleting the row is worse than useless because the next sweep re-inserts it as a fresh draft. Both decisions stamp the column, and `persistScraped` writes neither it nor `active` on re-scrape, so a decision sticks forever (verified on prod incl. a simulated re-scrape). **The queue is scoped by SOURCE, never by `active` alone** — `--hide` flips `active` off across all ~800 scraped rows, and an `active=false` queue would then present every published event as awaiting review. Finished events are excluded by the same rule as `pruneFinished` (end date where present, city-local), so a running exhibition stays a live decision; the card shows the **date range** because an exhibition rendered as its opening day looks exactly like the mis-parse the queue exists to catch. **Still unverified: the rendered UI** — `isAdmin` redirects a headless session, so data/API/auth are proven but the visual layout is not.
  - **✅ DONE — the For-you feed pages past 60.** The API returns up to `MAX_RETURNED = 240` ranked events; the client reveals `PAGE = 60` at a time and then says `Showing the top 240 of 753` rather than ending silently. **Paging is deliberately client-side** — a page param would re-run `extractPersona` per page, turning a free scroll into a model call each time (the once-per-item rule). 240 events ≈ 120KB raw / 24KB gzipped.
  - **✅ DONE — countdown badges age themselves** (no polling, no model call). Elapsed time is measured **entirely in the browser** (`start` captured in an effect, compared against the same clock); the absolute countdown still comes from the server. **Never subtract the server clock from the client clock** — a phone an hour off would read that offset as an hour elapsed and declare tonight over, which is the exact bug that keeps `startsInMin` server-side.
  - Signed-out users see "Current location" rather than a neighbourhood — `/api/places/reverse` is auth-gated on purpose (each call is a paid Google request). Relaxing it is a cost decision.
  - ~~5 events unlabelled~~ — **cleared** by the verified prod sweep (2026-08-04). `community-civic` still lands on ~65% as a catch-all (down-weighted to 0.35 in ranking, but it dilutes).
  - **Operational:** kill switch is `npx tsx scripts/publish-events.ts --hide` (`--show` to restore) — pulls every scraped event from the feed in ~1s with no deploy. Re-scrape: `npx tsx scripts/publish-events.ts`. Label new: `npx tsx scripts/label-events.ts`.
- **Security scan follow-ups (2026-07-22).** ✅ **Second batch done:** rate-limited `/api/ai/extract`, `/api/vendor/assistant/tune` + `/voice`, `/api/push/register` + `/test`; `/joindemo` password is now **fail-closed in prod** (no default — needs `JOINDEMO_PASSWORD`); JSON-LD `</script>` escaped; email `ctaUrl` escaped; **PostHog error tracking enabled** (`capture_exceptions:true`). ✅ **Admin demo is no longer public** — `isDemoActive()` (`lib/demo-server.ts`) now requires, in production, BOTH the `demo_admin_type` cookie AND the shared demo-password unlock (`JOINDEMO_COOKIE`); the `/demo` launcher prompts for the password (POST `/api/joindemo/unlock`) unless `NEXT_PUBLIC_DEMO_MODE=1`. **One password (`JOINDEMO_PASSWORD`) now gates both `/joindemo` and the admin portal preview; it is SET on CapRover prod (`WhatsLocalDemo2026!` — change anytime in the CapRover dashboard).** Writes still no-op regardless. **Deliberately NOT changed:** `import 'server-only'` on `lib/api.ts` would BREAK the build — client components (`EventThread`, `ShareComposer`) import its public `listMembers`/`listEvents`; the token is already stripped to `undefined` client-side. Real fix = split client/server exports (deferred refactor, not a hole today). **Still open (low):** transitive `sharp`/libvips + `postcss` audit highs (build/image-time; `next` patched to 16.2.11); in-memory rate limiter resets on deploy (fine single-instance, needs Redis if multi); verbose 500 `error.message` in some routes.
- **Ad pixels: only the IDs are missing (2026-07-22).** All code is built + wired (reverse proxy, Meta/Google pixels, consent banner, `trackConversion`) — the pixels just no-op until the three IDs are set. **TODO:** create the ad accounts, then set `NEXT_PUBLIC_META_PIXEL_ID` + `NEXT_PUBLIC_GA4_ID` (`G-…`) + `NEXT_PUBLIC_GOOGLE_ADS_ID` (`AW-…`) in `.env.local` **and on CapRover** (⚠️ `NEXT_PUBLIC_*` is baked at build time — must be set before the prod build), then rebuild. Placeholders + where-to-find-each are already in `.env.local`. Optional follow-up: drop `trackConversion()` calls at signup / business-claim / subscribe / checkout / RSVP so ad platforms have real conversion events + can build audiences. Session replay is **already ON** (see the PostHog env note below) — it is not on this TODO list; what is still unset here is only the three ad-pixel IDs. **⚠️ iOS App Tracking Transparency (ATT) — REQUIRED before enabling the Meta/Google pixels on the App Store build.** The Meta Pixel + Google Ads tags link data to third-party ad graphs = "tracking" per Apple's definition, so turning them on in the native app requires: (1) an ATT system prompt gating them (Capacitor: `@capacitor-community/app-tracking-transparency`; fire the pixels only if the user Allows), and (2) declaring Tracking in App Store Connect → App Privacy. Skipping this after enabling pixels = rejection (5.1.2 / 3.2.2). As submitted the pixels are OFF (IDs unset), so no ATT was needed — the 2026-07-29 App Review cookies reply (`scratchpad/asc-replies.md`) truthfully states no tracking occurs. If ads are ever turned on for iOS, wire ATT + the privacy declaration FIRST.
- **Uber Direct: creds SET + verified; BLOCKED on Uber account activation (2026-07-31).** Real credentials from a self-serve Uber Direct account are now in `.env.local` (`UBER_DIRECT_CUSTOMER_ID=e2b28359-381e-4d64-8274-1109b8f7a732` + `UBER_DIRECT_CLIENT_ID` + `UBER_DIRECT_CLIENT_SECRET`; webhook secret still blank/optional). **Verified:** OAuth probe → 200, real 30-day `eats.deliveries` token (so API access is already granted — no sales email needed). But a real SF→SF `delivery_quotes` probe → **HTTP 400 "This account has been disabled. Please reach out to directbilling-group@uber.com"** — an account-level (not auth) error, so all 3 creds are wired right; the block is Uber-side **billing activation** (signed up with a personal email; likely the credit-card step wasn't completed). **Action:** email `directbilling-group@uber.com` (draft in memory) to activate. **⚠️ Do NOT push `UBER_DIRECT_*` to CapRover/prod until the quote returns a real fee** — otherwise a buyer choosing delivery hits "account disabled" at dispatch. Re-run the quote probe once Uber enables it; fee instead of 400 = truly live → then push to prod. See memory [[uber-direct-delivery]] for the full state + delivery-option tradeoffs (why we don't switch to Shipday/Nash). Still unproven end-to-end once activated: a real dispatch, the fee delta in practice, the webhook lifecycle.
- **Services still fulfil approximately (2026-07-17, partly fixed).** `products` has no type/kind and no inventory column, so a vendor can list "1-hour consultation, $100" and take payment (the seed data already sells a $150 Energy Consultation). The delivery-modal-for-a-consultation problem is **gone** (fulfillment is chosen before payment, and a pickup-only vendor never shows delivery), but a service still reads as "Pick up" + "Mark Ready", which is only roughly right. Real fix = a product `kind` (good | service); anything scheduled additionally needs a booking system, which does not exist.
- **Collab lock-in has never been walked by a real signed-in vendor (2026-07-16).** The room UI (locked lineup, "Request to join") renders only for a real Clerk vendor — the demo swaps in the scripted `CollabRoomDemo`, so `Thread` is never exercised there. The API contract is verified (409 locked / 200 unlocked / correct `lineup` states) and the UI was rendered through a throwaway harness, but **nobody has clicked "Request to join" as an actual business**. Do that before trusting it in front of vendors.
- **A join request notifies nobody (2026-07-16).** A self-join lands in the host's Organize → Lineup tab, but nothing tells them — so requests can sit unseen. Pre-existing for the public event join link too. `notifyMemberSafe` is already wired for lineup invites; it's ~5 lines in `app/api/events/join`, held back only because that endpoint is shared by three flows.
- **`AdminPanel` Places search still debounces (350ms) rather than using a button** (2026-07-16) — admin-only and it now shares `lib/places.ts`'s 24h cache, so it's cheap. Convert if it ever matters.
- **Unread read-state → move server-side when collabs get real usage (deferred 2026-07-16, deliberate).** Badges work now, but read-state is **per-device** (localStorage) and `/api/vendor/activity` ships up to **2000 message rows every 30s just to count them** — fine at this scale, won't scale. The fix is ONE migration into homes that already exist: `collab_room_members.last_read_at` (per member per room) + `chat_conversations.owner_read_at` (single reader = the owner), then count server-side and return counts instead of rows. **The UI wouldn't change** — swap `lib/unread.ts` only. Do this before real collab volume, not before.
- **`inferRole()` has no correction path** (2026-07-16) — an event lineup role is inferred at invite time and the Lineup tab can't edit it, so a brewery-as-sponsor stays mis-roled. Cheap fix if it bites: make the role chip on each participant row tappable to reassign.
- **Ship the community/organizer/onboarding/NFC session** (built; mostly shipped):
  1. ✅ **DONE** `supabase db push` — all 12 pending migrations applied (this session's `…130000`–`…190000` + 5 older). The xeno project had auto-paused; restore it first if pushes time out again.
  2. **Email (Resend):** ✅ **DONE (2026-07-08); re-verified live 2026-07-22** — `RESEND_API_KEY` (send-only restricted key) + `RESEND_FROM="WhatsLocal AI <events@whatslocal.ai>"` set; the `whatslocal.ai` domain is verified and a live test send succeeded. Organizer email blasts + invite emails send. **Resend is the single email channel — no SMTP** (REST-only, serverless-friendly, one verified domain). SMS reuses `CONNECTOR_URL` + `CONNECTOR_ADMIN_TOKEN`. See the **Notifications** convention in Key Conventions (`lib/notify.ts` = push + email).
  3. ✅ **DONE** Deployed the connector-agent (`marketplace-create-member` + `marketplace-onboard`) to prod via Netlify CLI (files-on-disk deploy). ✅ **The "uncommitted WIP" is now committed (2026-07-16, `8fd4077` on `feat/trusted-number-enrichment`)** — it was never *unshipped*, just uncommitted: probing prod found `chat-stream`/`otp-request`/`voice-menu`/`member-reset` all live (401/200, no 404s), i.e. **git was ~2.5 weeks behind prod**, not ahead. Files-on-disk deploys are why. Token chain **verified**: marketplace prod `CONNECTOR_ADMIN_TOKEN` == connector prod `ADMIN_TOKEN` (authed probe got 400 "name required", past auth). Also set `CONNECTOR_ADMIN_TOKEN`+`CONNECTOR_URL` in local `.env.local` (were commented out → would've sent `Bearer undefined`). Member creation now works end-to-end. **(Caveat: `organizerFocus` assumes the connector stores/returns free-form profile fields like the facets do — confirm it round-trips.)**
  4. ✅ **DONE** marketplace deployed to Netlify prod (multiple times this session). The iOS app loads the hosted site (`server.url`), so web changes ship via `netlify deploy --build --prod`, not `cap sync`.
  5. **Native iOS NFC** (`whatslocal-ios`): staged (local plugin `local-plugins/capacitor-nfc/`, synced into SPM + `packageClassList`, Info.plist + `App.entitlements`). **Apple Developer Program membership is now ACTIVE** (2026-07-08) — the prior blocker is cleared. Remaining: Xcode → App target → Signing & Capabilities → + "Near Field Communication Tag Reading" → rebuild on a **physical** device. Entitlement is NOT yet linked in the pbxproj, so current builds are unaffected. See `whatslocal-ios/local-plugins/capacitor-nfc/README.md`.
  6. **Optional follow-ups** (offered, not built): server-side facet filtering on the connector (`marketplace-members` accepting `businessSize`/`ownership`); auto-tag QR self-onboards with the event's group tag; one-click "Added" on a join request → connector create; NFC **write** (vendor writes their URL to a blank tag); attendee SMS/email blasts (separate from the vendor lineup); paid event tickets (Stripe-Connect-per-organizer).
- **Composio + Trigger.dev setup before commerce sync works** (code is built; it no-ops until configured):
  1. Set `COMPOSIO_API_KEY` + `COMPOSIO_SHOPIFY_AUTH_CONFIG_ID` + `COMPOSIO_SQUARE_AUTH_CONFIG_ID` in `.env.local` (placeholders added). Until then connect/sync/push are no-ops.
  2. **Trigger.dev** (interactive — run yourself): `npx trigger.dev login` → `npx trigger.dev init` (creates this repo's own project; writes the ref). Put `TRIGGER_PROJECT_REF` + `TRIGGER_SECRET_KEY` in `.env.local`. Dev worker: `npm run trigger:dev`. Deploy: `npm run trigger:deploy`. Without `TRIGGER_SECRET_KEY`, "Sync Now" still works via an inline sync; only the daily cron needs the deployed worker.
  3. Connector-agent cleanup (separate session): delete the old `composio-*.js` + `lib/composio.js`/`lib/supabase.js`, remove `COMPOSIO_*`/`SUPABASE_*` env there.
- **AI features setup before they work** (only `OPENAI_API_KEY` + a DB push needed; bucket is a migration now):
  1. Set `OPENAI_API_KEY` in `.env.local` (placeholder already added). Optional: `ADMIN_CLERK_USER_IDS`, `SUPABASE_SERVICE_ROLE_KEY`.
  2. Apply migrations: `supabase link --project-ref xbbnvkvlrucrzobhopgh` then `supabase db push`. New migrations: `20260605120000` (assistant tables), `20260605140000` (vendor_events), `20260605150000` (public `marketplace-media` storage bucket + policies).
  - Roadmap: `/Users/xen/.claude/plans/eager-splashing-kazoo.md` — **Phase 1 (assistant) + Phase 2 (image→catalog capture) shipped; Phase 3 = voice receptionist** remains.
- **Set `NEXT_PUBLIC_SITE_URL=https://whatslocal.ai` in the production/deploy env** — it's only in local `.env.local`. Without it, prod canonicals/sitemap/JSON-LD fall back to the `https://whatslocal.ai` default in `lib/seo.ts` (currently correct, but make it explicit per environment).
- **Repoint `whatslocal.ai` DNS to this app** — it currently serves a different app, so none of the SEO output is publicly reachable yet.
- **(Optional) Per-member OG images** via `@vercel/og` — deferred nice-to-have for richer link previews.
- **Resources hub — populate the real catalog (`lib/resources.ts`).** v1 ships with a 5-item placeholder seed; the engineering (page, search, cards, recs, chat) is done but the *content* is the remaining value. Add real, verified SF entries (real `url`, or leave `url: null` + `searchHint` for a search-fallback link — never fabricate a link). Still to add from the original brief: legal/law clinics, accounting/bookkeeping help, energy-bill savings programs, planning & renovation/permit deals, business news/research (small business center), business directories, business plans, market research, entrepreneur support orgs (SCORE / Renaissance / MEDA), library events, certified small business (state/LBE) registration, SF Office of Small Business permits & events, small business public library. Induction / green / accessibility entries exist as placeholders and need real links/details.
- **Resources hub — improve-later (already shaped for, no rewrite):** LLM "why this fits you" blurbs; save/dismiss resources (needs a table **with anon/authenticated/service_role grants**); multi-city (`city` field); richer match signals (`tags`/`recommendFor`).
